
done: created model, crud-routes and validation for voters, and organize routes

1. **Model**:
    - create a voter model to the application, for schema structure.
2. **Routes**:
    - create a Index and Show route in one and uses data-table, for viewing existing voter's accounts.
    - create a POST route for creating voter's accounts.
    - create a PATCH route for updating voter's information.
    - create a DELETE route for deleting specific voters.
3. **Validation**: 
    - Update Joi, for validating voterSchema.
    - Added a new middleware function, `validateVoter`, for validating voter's data before submission.
4. **Express Routing**: 
    - Organize routes for voters with Express Router, for better readability.
